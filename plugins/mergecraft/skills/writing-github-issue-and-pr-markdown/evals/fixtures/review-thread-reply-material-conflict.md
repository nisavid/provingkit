# Review-thread reply with conflicting consumer policy

Field: review-thread reply.

The requested evidence-backed reply would explain in one long paragraph that the new regression test covers both a timeout before object creation and an ambiguous timeout after a possible write.

An applicable consumer instruction requires every comment paragraph to be physically wrapped at 72 columns and requires a backslash hard break between the two timeout cases. It says those source-shape rules are mandatory and may not be replaced by HTML.

Use LF and no terminal newline. Do not post a reply or alter the thread.
