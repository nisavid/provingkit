# Review-thread root with compatible instructions

Field: review-thread root.

Applicable consumer instructions require direct language, the literal prefix `Suggestion:`, no first-person pronouns, and a link to `https://example.test/spec#identity`. They set no source-width, newline, or preservation rule.

Author one review-thread root that asks the author to bind the reconciliation lookup to repository, pull request, operation, source revision, and intent key because identical bodies on different sources must not alias. Use LF and no terminal newline.

Do not create a review thread or submit a review.
