# Changelog

## Unreleased

- Treat broad subprocess launch-or-communication failures as unknown process
  outcomes, preserving mutation ambiguity, exact rereads, and nonce recovery.
- Bound shared review-input loading by bytes, nesting, structure, and numeric
  digits; reject non-finite numbers and invalid scalar text with one value-free
  chained admission error.
- Admit create, text, ready, reconciliation, and comment local inputs before
  validator or external effects; keep diagnostics value-free while retaining
  their original causes and local remediation.
- Retain initial-create process facts and exact nonce-recovery observations
  without assigning causality, minting receipts, or changing retry authority.
- Report each safe reread as intended, preimage, another valid state, or
  unavailable after canonical create, text, ready, and snapshot-cleanup failures.
- Admit every receipt string and object key as UTF-8 scalar text before canonical
  encoding, and contain malformed receipt text as value-free unavailable audit.
- Classify local process preparation, exact REST recovery bodies, and every
  zero-exit nonmatching publication reread without assigning write causality.
- Bound provider PR identifiers and JSON integer conversion, and admit only exact
  UTF-8 scalar live fields across recovery, audit, and receipt consumers.
- Admit complete, structurally valid live PR observations before audit comparison;
  classify invalid JSON or fields as unavailable and reserve drift for valid state.
- Require strict UTF-8 for successful publication-helper output, preserve safe
  process metadata for failures, and keep duplicate-key data out of diagnostics.
- Preserve the original create/update failure and a failed recovery-read
  classification without retrying, minting provenance, or writing a receipt.
- Safely classify subprocess launch failures, canonicalize provider-reported PR
  and comment URLs, and retain zero-exit mutation context when verification
  rereads fail.
- Reuse valid existing read authority during recovery and give local candidate
  validation failures value-free validator guidance instead of forge-access
  advice.
- Close comment acknowledgements over safe verified fields and retain zero-exit
  comment verification context without exposing body or provider bytes.
- Keep nonzero comment outcomes unknown without retry and validate display-safe
  expected and observed GitHub.com actor logins before mutation or output.
- Preserve safe audit-read classifications and translate private body-snapshot
  preparation and cleanup failures without losing mutation or error context,
  including initial-create cleanup failures with no recovered draft.
- Detach retained live-state JSON at a bounded structural boundary, reject
  ambiguous number/URL and repository identities, and type parser-limit,
  non-finite, direct audit-reader, and specialist-input failures without values.
- Keep comment-stage timeout, launch, nonzero, and malformed-success facts, and
  contain receipt-acknowledgement failures after a possible completed mutation
  with explicit audit-without-retry guidance.

## 1.0.0

- Add the portable seven-field GitHub Markdown authoring skill, generated
  writer-local projections, conflict-safe exact-body ownership, and raw behavior
  and routing corpora.
- Use Agent Plugins v1 as the canonical package format, with a native Claude
  manifest projection and skill-local Codex metadata.
- Bind create, text, and ready publication to an explicit review mode, exact
  publication candidate, trust-anchored Task Witness observation, and v3
  append-only receipt; make required mode fail closed when that chain is absent.
- Carry the exact review mode and publication-candidate digest through Graphite
  schema-v2 repair so legacy, reconciled, or weaker receipts cannot converge.
- Reconcile writer and publisher behavior with the pinned personal source:
  reviewer-visible merge-base statistics, grammatical and executable navigation,
  field-scoped mutations, explicit extension and concurrency boundaries, and
  retained behavioral and routing evals.
- Publish portable Mergecraft PR lifecycle skills and validation corpus.
- Reconcile the review-writing cluster with the pinned personal source at dotfiles
  f700471: fix failure narratives and shared-scenario deduplication in the
  writer contract, the clean-diff approval request for CodeRabbit, and ledger
  dispositions for the reviewer-side succession and writing-style ownership.
