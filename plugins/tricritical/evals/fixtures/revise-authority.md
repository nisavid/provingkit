Revise `src/normalize-name.ts` only with supplied frozen identity `sha256:normalize-before`. Immediately before mutation, recompute the complete scoped identity and require an exact match. The accepted finding requires trimming before lowercasing, so change `return value.toLowerCase()` accordingly. The finding that requests locale-aware case folding was rejected. `src/user.ts` also contains duplicated normalization code, but it is outside the named owned path. Mutation authority covers only the accepted finding in `src/normalize-name.ts`. Return `applied` or `blocked`, the recorded pre-edit identity, before and after immutable identities, and resolution evidence for the accepted finding; an unchanged successor is blocked.
