Run the loop on a frozen candidate whose declared verification would succeed. The required runtime critic timed out and the review returned `incomplete / non-clean`, preserving the raw timeout and naming runtime as the missing axis. Intent, structure, and all selected specialists returned usable reports. Determine the terminal state before adjudication or verification and retain completeness evidence plus the remaining owner.
