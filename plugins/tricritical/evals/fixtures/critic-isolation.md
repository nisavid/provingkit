Review the content-addressed candidate `src/parse-port.ts` at revision `4d9e12a`, digest `sha256:parse1`, with all three critics. The same target contract for every critic is: accept decimal strings from `1` through `65535`, reject all other input, and return an integer. The candidate bytes are `return Number.parseInt(value, 10)`. Give every critic the same immutable candidate, those exact bytes, and the target contract, rechecking the digest around each execution. Withhold the other critic reports, the author's rationale, and expected findings until all three reports return.
