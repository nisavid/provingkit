Review immutable candidate `src/calculate-total.ts` at commit `7a4b2c1`. The target contract says `calculateTotal` must reject negative prices and return the subtotal plus a decimal tax rate. The candidate currently returns `prices.reduce((sum, price) => sum + price, 0) * taxRate`. Mutation authority is explicitly absent. Run the intent, runtime, and structure axes in isolated contexts, then return their reports and a synthesis.
