A successor changes runtime bytes and runtime evidence for one parser path. The
frozen intent scope and its dependency identities are unchanged, and a prior
usable intent report plus an explicit dependency proof are available. Structure
is not a material concern in this current increment, and no security authority
surface changed. Choose which scope needs a fresh pass, what exact evidence can
retain the unchanged intent scope, and why the plan must not add unrelated
review axes or specialists.
