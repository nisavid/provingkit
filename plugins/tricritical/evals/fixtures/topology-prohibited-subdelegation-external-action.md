Run Tricritical review for immutable candidate `sha256:candidate-c` with review input `sha256:input-c` and requirements `sha256:req-c`. The frozen receipt contains exactly one entry for each selected critic. The intent entry is read-only, but grants the worker permission to create another worker and post a top-level pull-request comment if useful. The other entries deny both powers. All model selections are separately proven and no execution has begun. Decide whether any part of the plan may dispatch and what terminal evidence must be preserved.
