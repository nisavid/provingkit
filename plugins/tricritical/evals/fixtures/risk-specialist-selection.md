Plan Tricritical review executions for a frozen candidate that changes an OAuth authority check, rewrites persisted token rows during upgrade, and changes the release rollback sequence. The fixed comparison boundary contains no UI, accessibility, performance, or concurrency behavior. Select and isolate only the consequence-driven read-only risk specialists, giving each the same frozen input and candidate bytes.
