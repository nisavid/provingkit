Run Tricritical review for immutable candidate `sha256:candidate-b` with review input `sha256:input-b` and an explicit absence of requirements. The selected intent, runtime, and structure entries target a foreign harness. Its live capability probe shows that all peers share one conversation, can see prior reports, and cannot enforce read-only access or deny subdelegation and external actions. The receipt nevertheless labels the executions distinct. Native execution might exist, but no replacement plan has been issued. Determine whether dispatch or fallback is permitted and the resulting completeness state.
