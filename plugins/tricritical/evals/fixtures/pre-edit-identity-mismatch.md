Revise scoped candidate `src/normalize.ts`. The frozen contract supplies identity `sha256:frozen-before`, but an immediate recomputation over every scoped path, entry type, mode, and byte yields `sha256:changed-before`. One accepted finding requests trimming an input, and mutation authority otherwise covers the file. Record the recomputed pre-edit identity, return the identity gate result, and make zero edits when the identities differ.
