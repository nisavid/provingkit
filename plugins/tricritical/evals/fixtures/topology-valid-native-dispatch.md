Run Tricritical review for immutable candidate `sha256:candidate-d`, review input `sha256:input-d`, and requirements `sha256:req-d`. Consequence analysis selects intent, runtime, structure, and one persistence specialist routed through runtime. A Rolecasting receipt binds those identities and contains exactly four unique native-subagent entries, each with a distinct isolated context, the same immutable inputs, bounded scope, read-only authority, denied subdelegation and external action, a return shape, verification, and stop conditions. Four separate capability-proven model-selection receipts match the entries. Determine the permitted dispatch set, fallback boundary, and completeness accounting.
