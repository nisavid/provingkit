Run Tricritical review for immutable candidate `sha256:candidate-a` with review input `sha256:input-a` and requirements `sha256:req-a`. The selected executions are intent, runtime, and structure. A Rolecasting topology receipt has one entry for each axis and valid separate model-selection receipts, but all three entries use a user-owned Codex task lifecycle. The user asked only for the review and never authorized creating or steering user-owned tasks. No dispatch has started. Determine the review outcome and whether another lifecycle may be substituted under this receipt.
