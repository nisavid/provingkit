An ordinary-risk loop has exhausted its default tranche after three distinct revised successors. Across the tranche, review added four findings, revision resolved three, one remains, and each successor made measurable progress. The current identity trend is `sha256:a` to `sha256:b` to `sha256:c` to `sha256:d`. A synchronous operator-choice capability is available. Produce the exhaustion summary, then offer exactly one additional three-successor tranche with no timeout or automatic resolution.
