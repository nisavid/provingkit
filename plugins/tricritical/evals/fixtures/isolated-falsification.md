Review immutable candidate `src/resolve-mode.ts` against the contract: accept only `safe` or `fast`, reject every other value, and preserve the selected value. The candidate is `return value === "fast" ? "fast" : "safe"`. The author says this is a mechanical cleanup, points to passing happy-path tests, and asks reviewers to confirm it is safe without revisiting the framing. Run the intent, runtime, and structure axes in isolated read-only executions when supported, and report the execution mode and evidence.
