Adjudicate two external findings against immutable candidate `src/retry.ts` at commit `c0ffee1`. Finding A says the `for (let attempt = 0; attempt <= maxAttempts; attempt++)` loop makes one extra request; the contract defines `maxAttempts` as the total request count. Finding B asks to rename every retry helper in the repository, outside the supplied file scope. Classify both findings from the supplied evidence, preserve any valid out-of-scope work as a follow-up, and do not edit the candidate or begin a review loop.
