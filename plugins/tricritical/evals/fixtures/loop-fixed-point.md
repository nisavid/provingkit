Run the fixed-point workflow on content-addressed `src/read-timeout.ts`. The frozen current increment authorizes returning a finite integer number of milliseconds from `1` through `30000`, claims that configured and default values satisfy that range, supports absent or decimal-integer `TIMEOUT_MS` inputs, sets boundary tests and unchanged-byte verification as acceptance criteria, and selects intent plus runtime reviewer scopes over named dependencies. The current candidate is `return Number(environment.TIMEOUT_MS ?? 5000)`. Mutation authority covers only that file, and the declared verification is `npm test -- read-timeout`. Pass the supplied frozen identity to revision, record identity before and after verification, and block on drift. Successful verification of unchanged final bytes is required even if review accepts the initial candidate. Begin every successor cycle from a fresh immutable candidate, preserve any unresolved finding, and terminate an unchanged or no-progress successor instead of repeating.
