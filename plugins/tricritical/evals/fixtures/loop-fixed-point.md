Run the fixed-point workflow on content-addressed `src/read-timeout.ts`, whose target contract requires a finite integer number of milliseconds from `1` through `30000`. Record its size, complexity, and consequence basis, then use the caller's finite positive integer override of two revised successors. The current candidate is `return Number(environment.TIMEOUT_MS ?? 5000)`. Mutation authority covers only that file. The operator-authorized frozen contract declares non-mutating verification `npm test -- read-timeout`. Pass the supplied frozen identity to revision, record identity before and after verification, and block on drift. Successful verification of unchanged final bytes is required even if review accepts the initial candidate. Begin every successor cycle from a fresh immutable candidate. Tranche exhaustion returns the findings and identity-progress summary, and an unchanged or no-progress successor terminates instead of repeating.
