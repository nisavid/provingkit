Synthesize independent reports that disagree about a frozen null-handling branch. The runtime report says `profile.owner.name` is always dereferenced; the structure report says the branch returns when `profile.owner` is absent. The frozen source excerpt includes `if (!profile.owner) return;` immediately before the dereference. Resolve only what that source proves and retain any uncertainty.
