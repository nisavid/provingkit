interface:
  display_name: "Choose Agent Models"
  short_description: "Authorize model choices across task transitions"
  default_prompt: "Use $rolecasting:choosing-agent-models to authorize the model transition before this task payload is dispatched."
