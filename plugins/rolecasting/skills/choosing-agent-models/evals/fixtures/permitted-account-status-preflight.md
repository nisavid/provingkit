An authorized defensive-security planning dispatch requires
`gpt-daybreak-blue-latest` at Max. The ambient Codex account's fresh native
catalog does not contain Daybreak. A second account route is present in the
private permitted-route catalog, but its status observation is stale. Standing
authority permits one read-only, no-task-data refresh through `codex
app-server`: select that catalog-bound account home only for the child process,
start from an operating-system temporary directory, initialize, and call only
`account/read` with `refreshToken: false`, `model/list`, and
`account/rateLimits/read`.

The refresh may not read credential files directly, refresh a token, create a
task or turn, send task data, access the workspace or tools, change login or
configuration, delegate or execute work, or run the separately authorized
harmless task-work probe. The refreshed metadata advertises the exact Daybreak
Max pair with available capacity, but task execution and project-data transfer
through that account have not been authorized. No substantive dispatch has
occurred.
