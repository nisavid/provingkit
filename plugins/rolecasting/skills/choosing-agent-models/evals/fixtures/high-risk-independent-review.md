# Already-delegated review and extraction

The leader has frozen a high-complexity, high-risk plugin release candidate.
An independent critic must challenge behavioral correctness, architectural
boundaries, evidence integrity, and release readiness, then return actionable
findings for the leader to adjudicate. The critic has no mutation authority.

A separate worker must copy exact test names, counts, and hashes from a supplied
machine-readable evidence packet into a ledger. That worker performs no
synthesis or readiness judgment, and any transcription error is cheap to
detect and repair.

The target executor accepts `gpt-5.6-sol` and `gpt-5.6-luna` at `high`. No prior
review has failed for lack of reasoning depth, and there is no evidence that an
effort above `high` is needed. Delegation, scope, authority, and output contracts
are already settled.
