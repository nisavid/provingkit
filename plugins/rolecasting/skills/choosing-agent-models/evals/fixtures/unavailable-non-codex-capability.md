# Unavailable Non-Codex Capability

Delegation is settled on a non-Codex harness. Its executable is present, but the version probe exits nonzero and the model-catalog output is malformed and truncated, so the requested slug and effort cannot be parsed. No executor schema establishes accepted selection fields, no alternative surface is authorized, and the missing capability evidence could be supplied after the harness is repaired. Do not use tools.
