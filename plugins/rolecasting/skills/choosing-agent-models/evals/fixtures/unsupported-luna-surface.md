# Raw scenario

The leader has already assigned a bounded extraction task whose errors are
cheap to detect. Fresh catalog evidence lists Luna, Terra, and Sol, but the
target native-subagent schema accepts only `gpt-5.6-terra` and `gpt-5.6-sol`.
For Terra, the schema accepts `low` and `medium` effort. The operator has not
authorized another worker surface. Choose an executable model and effort and
state any material limitation.
