# Raw scenario

The leader has already assigned a bounded extraction task whose errors are
cheap to detect. Fresh catalog evidence lists `gpt-5.6-luna` at `xhigh` and
`gpt-6-astra` at `low`, but the target native-subagent schema accepts only `gpt-6-astra` at `low`, `medium`,
and `high` effort. The operator has not authorized another worker surface.
Choose an executable model and effort and state any material limitation.
