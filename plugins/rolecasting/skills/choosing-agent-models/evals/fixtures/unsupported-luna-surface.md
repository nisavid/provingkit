# Raw scenario

The leader has assigned a bounded extraction task whose errors are cheap to
detect. Fresh catalog evidence lists `gpt-6-luna` at `xhigh` and `gpt-6-sol`
at `high`, but the target native-subagent schema accepts only `gpt-6-sol` at
`high`. The operator has not authorized another worker surface. Choose an
executable model and effort and state any material limitation.
