# Inherited Fixed Model

Delegation is settled for a short, recoverable implementation task. The target executor exposes no model or reasoning-effort selection fields. The environment is fixed to an appropriate Terra model, and the task's scope, authority, expected output, and verification remain unchanged when that inherited model is used. Do not use tools or attempt another execution surface.
