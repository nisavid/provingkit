# Raw scenario

The user asks the leader to parallelize a short, tightly scoped same-harness
repository inspection. They do not ask for a new Codex task, thread, or chat.
The native subagent surface is available, and the assignment needs no separate
lifecycle or worktree. Choose the worker lifecycle and state what the leader may
create.
