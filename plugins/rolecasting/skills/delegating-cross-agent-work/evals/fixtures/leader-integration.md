# Raw scenario

Two native subagents worked in disjoint edit scopes from a recorded immutable
base and both returned `DONE`. One changed an interface and the other added tests
against the previous interface, so their claims conflict when viewed together.
Nothing has been integrated or verified in the owning worktree. State the
leader's next actions and the completion gate.
