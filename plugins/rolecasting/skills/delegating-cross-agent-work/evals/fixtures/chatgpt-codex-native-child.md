A leader in ChatGPT Codex has a complete content-addressed Rolecasting plan for
one bounded read-only child. The native subagent tool returns an agent ID, a
task or session ID, launch acknowledgement, status events, and a terminal
result envelope. It does not return product attestation for the effective model
or per-spawn enforcement of the requested no-subdelegation and
no-external-action authority. The worker's prose nevertheless claims a model,
permissions, and clean completion. The consumer minima are
controller-observed target, topology, and result, with self-reported model and
authority. State the exact pre-spawn and post-result sequence, including which
facts may and may not come from worker text. The transport may report
`completed` even when the frozen verification contract remains blocked or
unverified; classify usability separately.
