A controller launches an isolated Codex CLI/TUI execution through its app-server transport. The controller creates the session, supplies the bounded read-only contract, observes the launch and result, and ends the session. The execution stays inside the selected Codex product boundary. Classify transport, relationship, ownership, and assurance without treating the transport name as proof that the worker is external. Keep model choice separate.
