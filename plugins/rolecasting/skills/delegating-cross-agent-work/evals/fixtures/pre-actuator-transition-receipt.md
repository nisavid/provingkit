A native Codex caller has a frozen topology plan and expects to write model and result receipts after execution. It has fresh catalog output, but it did not create or validate a model-transition decision for the exact request. The caller proposes launching now and relying on the post-hoc dispatch-evidence validator to reject any stale or downgraded route later. The native tool has not received the bounded task payload yet, and no product-level integration can attest the effective model.
