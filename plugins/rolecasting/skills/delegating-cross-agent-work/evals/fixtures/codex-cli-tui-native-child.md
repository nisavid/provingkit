An interactive Codex CLI/TUI leader must delegate one isolated read-only child
without depending on app-server. The complete plan, request, target version,
executor, context, requested authority, and all five assurance minima are
known. The native tool exposes an agent ID, session or task ID, launch event,
status stream, and terminal result, but no effective-model attestation or
per-spawn authority-enforcement receipt. The minima permit self-reported model
and authority and require controller-observed target, topology, and result.
Explain the operational route, preserve nonclean terminals, and state the
publication boundary. A terminal `completed` status must remain distinct from
the same-leader verification observation and strict usability decision.
