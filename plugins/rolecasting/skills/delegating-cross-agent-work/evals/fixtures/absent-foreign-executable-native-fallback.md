# Absent Foreign Executable With Native Fallback

The requested foreign CLI is not installed, and its executable lookup reports no matching command. The task is read-only source inspection with a structured evidence return. A native subagent has the same repository access, isolation, authority boundary, output contract, and verification capability, so using it does not change the original task contract. The native executor exposes no explicit model or effort fields. Do not use tools.
