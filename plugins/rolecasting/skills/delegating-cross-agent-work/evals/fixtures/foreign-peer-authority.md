# Raw scenario

A foreign-harness peer is useful for a read-only browser QA pass against a local
preview. Its contract permits navigating the preview, capturing focused
evidence, and returning observations. During kickoff, the peer proposes spawning
another worker, changing product code, publishing screenshots, and deciding
which behavior should ship. Define its authority and the leader's retained
decisions.
