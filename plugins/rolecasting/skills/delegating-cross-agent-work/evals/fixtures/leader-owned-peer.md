A leader needs an independent browser-QA session while continuing implementation in the owning session. The leader can create, scope, observe, and end the QA session through a native task API. The QA worker receives read-only authority, cannot subdelegate or take external actions, and returns captures plus a short defect list. No user-owned task is requested. Classify the target, relationship, ownership, transport, assurance minimum, and the separate model-selection boundary.
