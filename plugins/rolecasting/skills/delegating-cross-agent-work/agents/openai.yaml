interface:
  display_name: "Delegate Cross-Agent Work"
  short_description: "Gate bounded cross-agent dispatches"
  default_prompt: "Use $rolecasting:delegating-cross-agent-work to freeze topology and enforce the transition gate for this bounded dispatch."
