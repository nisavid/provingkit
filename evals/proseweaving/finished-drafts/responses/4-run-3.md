## Reset exercise

This exercise uses a fixture. Nothing was run in production.

Setup: a stub closes the connection after the first request.
Observation: I observed the fixture exit 1.

The specimen includes this boilerplate: "That said, this is where reliability matters."

```sh
export-tool --retry-limit 3
```

1. In console A, run the command.
2. In console B, run the command.
