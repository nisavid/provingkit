Dependabot proposed an update for a component governed by an existing policy.
The repository, author, base, head namespace, changed pin path, requested
version, and current head all match. Independent assessment clears the exact
candidate under the unchanged policy, and the task delegates the bounded
update, same-branch amendment, review, approval, and merge handoffs.
