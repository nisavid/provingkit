The exact registry artifact digest is available, but no attestation was published. The repository policy explicitly permits acceptance without attestation only when the registry integrity record exactly matches, the canonical source tag resolves to the same source, metadata and license satisfy policy, and conformance checks pass. All four corroborating records are supplied and match.
