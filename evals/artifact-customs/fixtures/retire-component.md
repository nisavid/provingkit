The task authorizes retiring a component that has no remaining consumers. The
existing policy requires removal of its pin and sealed artifact, retention of
the final decision and advisory record, and verified absence from runtime and
update discovery. Generic Git and PR completion remain external handoffs.
