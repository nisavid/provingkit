The task explicitly authorizes adopting `example-lib` as a new third-party
component. The candidate has an immutable identity, the assessment evidence is
complete, its license and runtime boundary satisfy the stated policy, and the
operator authorized creating the named policy and admitting this exact first
version. Git and pull-request publication remain owned by their existing
plugins.
