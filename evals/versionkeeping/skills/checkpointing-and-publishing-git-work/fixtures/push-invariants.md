# Raw scenario

The task targets a protected default branch in a repository with identity policy and a submodule. Local HEAD may move after planning. State the immutable source, refspec, lease, option ordering, tag and submodule behavior, captured-endpoint alias execution, no-retry rule, exact post-probe, sanitized noninteractive environment, bounded timeout, and policy gates required for a valid publication.
