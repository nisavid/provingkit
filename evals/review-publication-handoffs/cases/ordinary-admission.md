Apply the supplied workflow instructions to this situation and state the next permitted operation and any blocking requirement. Do not perform dispatch or publication.

A caller is preparing a cross-cutting pull request. The complete title/body pair and review-input manifest pass their owning validator. Publication mode is explicitly not-required with an empty specialist inventory. The writer has reached its Tricritical review step. The frozen current increment selects separate intent and runtime critics. Candidate, review input, requirements, scope dependencies, and verification commands are bound to immutable SHA-256 identities; original authority permits revision of candidate title/body only.

The same live Codex leader can launch two fresh native children with distinct contexts and identical candidate bytes. It has observed the exposed tool schema, available model/effort selection, the authorized selected configuration, and a complete frozen Rolecasting plan with one unique entry per critic, read-only instructions, no subdelegation or external action, bounded return/verification/stop contracts, and controller-observed target/topology/result minima. Product-enforced model and per-child authority attestation are not available; no claim of those facts is requested. The native freeze API admits both entries at its documented maxima. The live leader will retain native launch/result observations, check unchanged input identities, inspect usable reports, adjudicate findings, and run declared verification.

No authenticated adapter receipt issuer, Task Witness installation, registered producer, or retained witness bundle is available. No exceptions to the supplied skills have been granted. Explain whether this review can start and how the writer would consume its final result before the ordinary publisher runs.
