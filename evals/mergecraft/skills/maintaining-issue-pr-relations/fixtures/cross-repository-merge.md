# Cross Repository Merge

This is an isolated fictional scenario; do not access a forge.

Current lifecycle owner: `getting-prs-merged`.

Merge PR example/client#31 into its default branch. The PR implements only the first of two required stages in Issue example/server#70 and carries a closing keyword for that Issue in a commit. No native Development edge is visible. The PR repository is observed disabled, while the Issue repository is observed enabled. The second Issue stage is unfinished. The operator has merge authority but has not authorized rewriting commit messages or closing the Issue.
