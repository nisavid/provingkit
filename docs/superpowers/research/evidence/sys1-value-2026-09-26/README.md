# Evidence for the supervision added-value comparison

This bundle preserves four fresh sessions comparing the concrete-blocker note
with no note on supplied and missing file evidence. Read the accompanying
[report](../../2026-09-26-sys1-supervision-added-value.md) and the preregistration
before interpreting the traces.

The executed runner and hook helper are byte-identical copies. `pilot-value-patch.json`
preserves the exact unified patch bytes as base64 with their SHA-256. Decode
`patch_b64` and verify that digest before applying it to the previous runner. Prepared settings, manifest, hook
logs, traces, summaries, and captured file bytes are retained under `pilot/`.
The execution freeze binds the original manifest and preregistration before
the four sessions. The adjudication distinguishes the baseline's conditional
request for the multiplier from the candidate's direct question.

Records use the same publication projection as the [earlier
bundle](../sys1-supervision-2026-09-26/README.md): machine paths are normalized,
encoded hook payloads receive the same transformation, and opaque thinking
signatures and unrelated initialization metadata are omitted. Relevant event
order, tool inputs/results, emitted notes, final responses, effects, and usage
remain. `file-inventory.json` binds public bytes and separately records original
hashes; original hashes are not hashes of the normalized projections.

The runner retains exact historical preflight pins. Normalized records cannot
serve as runnable qualification. Reproduction requires a fresh native
preflight, reviewed qualification record, updated pins, and a new frozen
preregistration, while preserving the old results. Keep the same native
controls, fresh sessions, one-time delivery after a successful brief read,
exclusive run records, and evidence capture before fixture reset. Adjudicate
actual information requests and file effects from the traces; do not infer
success from exit status or question-mark matching.

The reused qualification and its underlying observations are in the earlier
bundle. Its symlink-write attempt failed on an earlier read-before-write
condition, and it establishes no process-wide isolation. The fixed injected
signal and single observation per cell establish neither a general advantage
nor equivalence. No live hook or safety policy changed.
